# Utilities

Small shared helpers live here.

## Files

- `hashing.py`: stable IDs and content hashes.
- `text.py`: text normalization/tokenization helpers.
- `logging.py`: logging setup placeholder.

Keep this folder small. If a utility grows domain-specific, move it into the
relevant package.

